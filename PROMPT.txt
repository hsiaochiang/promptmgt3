# WOS PromptMgt - Master Handover Protocol

> **Role**: You are the Lead Developer for the WOS PromptMgt project.
> **Current Phase**: Phase 1 (Data Governance) & 30-Day Experiment.
> **Immediate Goal**: Fix the VS Code Extension to allow "Inbox Triage".

## 1. Project Navigation (Start Here)

The project documentation is organized in `docs/`. Please read these files to understand the "Why" and "How":

- **`docs/README.md`**: **MUST READ**. The master index of all documentation.
- **`docs/30_DAY_EXPERIMENT.md`**: The current strategic focus. We are doing a 30-day trial to validate utility.
- **`docs/WOS_TAXONOMY.md`**: The tagging system (Namespace Tags) used for data classification.
- **`docs/VERSION_CONTROL.md`**: Git workflow for code and data.

## 2. Architecture Overview (Monorepo)

The project is a Monorepo (`d:\program\promptmgt3`) containing:

### A. Backend (`apps/server`)
- **Stack**: Node.js, Fastify.
- **Data**: Markdown files with Frontmatter in `apps/server/data/`.
- **API**: Runs on `http://localhost:3001`.
- **Status**: Stable. `npm run dev` in root starts this.

### B. Frontend / Extension (`apps/vscode-extension`)
- **Stack**: VS Code Webview (HTML/CSS/JS).
- **Goal**: A sidebar interface to triage Inbox items.
- **Status**: **BROKEN**. Needs fix.
  - Issue: `node-fetch` integration issues in VS Code environment.
  - Task: Debug and fix execution so Wilson can use it.

## 3. Current Task for You (The Coding Agent)

**Context**: Wilson is trying to run the VS Code Extension (`WOS: Open Inbox Triage`) but it fails to execute or connect correctly.

**Action Items**:
1. **Read** `docs/README.md` and `docs/30_DAY_EXPERIMENT.md` to align with the "Minimalist/Experimental" mindset.
2. **Analyze** `apps/vscode-extension/` to fix the build/runtime errors.
3. **Verify** that the Extension can fetch data from `http://localhost:3001/api/inbox`.

## 4. User Constraints
- **Do NOT** over-engineer. Stick to the "30 Day Experiment" rules.
- **Do NOT** propose complex features (Phase 2-4 are paused).
- **Focus**: Make the "Inbox Triage" workflow smooth.

---

**System Ready.** Please proceed by analyzing the specific error in `apps/vscode-extension` while keeping the whole project context in mind.
