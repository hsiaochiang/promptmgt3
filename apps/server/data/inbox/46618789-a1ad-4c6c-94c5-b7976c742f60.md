---
id: 46618789-a1ad-4c6c-94c5-b7976c742f60
title: 分支 · 前端風格審查方法
importedAt: '2026-01-10T13:07:26.947Z'
cleanedState: unprocessed
suggestedTags: []
sourceLink: 'wos://archive/69546201-dda0-8320-9fb4-1c2f4bf55374'
sourcePlatform: chatgpt-export
notes: ''
project: beeecbb8-13c2-49e4-a98a-26250937264c
status: ACTIVE
category: '#Cat/PROMPT_DRAFT'
tags:
  - '#Stage/IDEA'
---
# 分支 · 前端風格審查方法

**Created**: 12/31/2025 07:36:40

## User

Original custom instructions no longer available

## User

如何在我以coding agent幫忙寫程式的過程中(VS Code中的Copilot或Codex)
能有一個腳色 幫忙看 目前的前端頁面的調整 是否是我期望的style
可以建立另一個agent嗎 還是skill 或是有其他方式

## AI

The output of this plugin was redacted.

## AI

The output of this plugin was redacted.

## AI

The output of this plugin was redacted.

## AI

The output of this plugin was redacted.

## AI

The output of this plugin was redacted.

## AI

The output of this plugin was redacted.

## AI

可以，但要先釐清一個現實：**Coding agent（Copilot / Codex）本質上主要讀程式碼與檔案**，它不一定「看得到」你瀏覽器中實際渲染後的畫面。因此，要讓它能判斷「是否符合你期待的 style」，通常要用「兩段式」設計：

1) **把你要的 style 變成可被引用的規範（文字化、可檢核）**  
2) **把實際 UI 變成可檢核的證據（例如 screenshot baseline / Storybook snapshots）**  
然後再讓 agent 扮演「Design Reviewer / UI Guardian」去比對規範與證據。

下面給你三種做法（由輕到重），每一種都能落地。

---

## 做法 A（最務實、成本最低）：用「Copilot 指令檔」建立固定的 Design Reviewer 角色

### A1. 建立你專案的 UI Style Guide（文字規範）
在 repo 放一份例如：`/docs/ui-style-guide.md`，內容至少包含：
- 視覺目標：Notion-like / B2B 金融風 / 極簡等
- Layout：grid、留白、最大寬度、sticky header 等
- Typography：字級階層、字重、行高
- 元件規範：按鈕、表格、tag、modal 的樣式與行為
- 禁止事項：例如「避免陰影過重」「避免漸層」「避免大圓角」等

### A2. 用 `.github/copilot-instructions.md` 固化「審美檢核角色」
VS Code 支援用 `.github/copilot-instructions.md` 讓 Copilot Chat 自動套用指令（需開啟設定 `github.copilot.chat.codeGeneration.useInstructionFiles`）。citeturn0search6turn1search4  
GitHub 也支援 repo custom instructions，並可用於 Copilot code review / coding agent。citeturn0search10turn1search0turn1search1

你可以把「Design Reviewer」規則直接寫進去，例如（示意）：

```md
# .github/copilot-instructions.md（節錄：UI Style Reviewer）

你同時扮演「UI Style Reviewer」：
- 任何前端 UI 改動（components/pages/styles/tailwind classes）都必須符合 /docs/ui-style-guide.md
- 請用 checklist 檢核：排版、留白、字級階層、元件一致性、互動狀態（hover/focus/disabled）、RWD、a11y
- 若無法從程式碼判斷視覺效果，請要求我提供：
  1) 對應頁面的截圖（或 Playwright baseline/diff 結果）
  2) Storybook story（若有）
- 回覆格式固定：
  1) 符合項目
  2) 不符合項目（逐點對照規範章節）
  3) 具體修改建議（以可直接貼回 code 的方式描述 class/元件調整）
```

### A3. 日常使用方式（你不用真的「開另一個 agent」）
你只要在 VS Code 裡把同一個 Copilot（或 Codex）用不同提示詞喚起不同模式即可：
- 「Builder」提示詞：請實作 xxx
- 「Design Reviewer」提示詞：請依照 ui-style-guide.md 審核我剛剛這次變更是否符合 Notion-like 風格，列出不符合點與修改建議

這種方式的優點是：**最省事**，缺點是：如果沒有截圖/可視化測試，agent 很容易只能從 class/元件結構做「推測」。

---

## 做法 B（更像你要的“另一個 agent”）：用 VS Code Copilot Agent Mode / OpenAI Codex 分工

### B1. Copilot Agent Mode（偏「多步任務的同事」）
VS Code 已提供 Copilot agent mode：能讀 repo、提案修改、跑 terminal / tests 等，適合把「審核 style」變成一個可重複任務流程。citeturn0search15turn0search11  
（若你未來要讓它自動去跑截圖測試、彙整差異，這種模式更順。）

### B2. OpenAI Codex IDE Extension（你可以把它當成另一個獨立工作同仁）
Codex VS Code extension 可在 IDE 內 side-by-side 使用，並支援不同 approval mode、可跑指令、也可 delegate 到 cloud。citeturn0search4turn1search2turn0search12  

實務上你可以分工：
- Copilot：負責快速修改 UI / refactor
- Codex：固定扮演「UI Style Reviewer」，每次你貼 PR diff 或指定檔案範圍，請它輸出 checklist 審核

> 關鍵：不論 Copilot 或 Codex，你都需要把「規範」放在 repo（A1/A2），否則 reviewer 的判斷標準會漂移。

---

## 做法 C（最可靠、最接近“客觀判定”）：導入視覺回歸測試，讓 agent 依結果判讀

如果你希望「像驗收一樣」判定是否符合 style，**視覺回歸測試（visual regression）**會是核心。

### C1. Playwright screenshot baseline/diff（頁面或元件）
Playwright Test 支援 `toHaveScreenshot()`：第一次產生 baseline，後續比對差異並產生 diff。citeturn0search0turn0search20  

你可以把流程定義成：
1) 你改完 UI  
2) 跑 Playwright screenshot tests  
3) 產生 baseline/actual/diff  
4) **Design Reviewer agent 讀測試結果與你對照 style guide，判斷差異是否可接受**

### C2. 若你有元件化：Storybook + Visual Tests（Chromatic）
Storybook 文件本身就把「Visual tests」定位為高效率的元件外觀驗證方式。citeturn2search1  
Chromatic 也提供以 baseline snapshots 做 commit-to-commit 視覺比對的工作流。citeturn2search0turn2search5turn2search9  

這套的好處是：你不只驗「頁面」，也能驗「元件狀態（hover/disabled/variants）」。

---

## 我建議你採用的組合（兼顧速度與可控性）
以你現在的情境（VS Code + Copilot/Codex、你很在意一致性與可交付）：

1) **先做 A（Style guide + copilot-instructions）**：讓「審美標準」固定化（立刻見效）citeturn0search6turn0search10  
2) **再加 C1（Playwright screenshot）**：讓「是否符合」有客觀證據citeturn0search0  
3) 若你已經有元件庫或準備長期維護，再上 **C2（Storybook/Chromatic）**citeturn2search1turn2search5  
4) 你若真的想要「另一個同仁」常駐並能跑任務，就用 **B2（Codex extension）**固定扮演 reviewerciteturn1search2turn0search4  

---

## 你下一步可以直接做的三件事
1) 在 repo 新增：`/docs/ui-style-guide.md`（我可以依你「Notion 風格」偏好幫你產一份 v1）  
2) 新增：`.github/copilot-instructions.md`（把「Design Reviewer checklist」寫進去）citeturn0search6turn0search10  
3) 挑 1~2 個關鍵頁面先導入 Playwright `toHaveScreenshot()` 當作 UI gate（先小範圍驗證流程）citeturn0search0  

如果你願意，我可以直接給你一份「可貼進 repo」的 `ui-style-guide.md + copilot-instructions.md` 初版（以 Notion-like、企業內部系統風格為基準），你只要再微調用詞與禁則即可。

## User

你的建議非常的好
但是我想先問幾個問題
1. PR 是什麼
2. skill是什麼 可以協助我做AI Coding嗎
3. 我打算在雛型階段把頁面、樣式 做到我想要的樣子 希望coding後的結果跟雛型一樣
4. 有些瀏覽器 或瀏覽器的外掛 是可以用LLM指揮的 例如點擊頁面 或輸入資料等等 這類的工具有幫助嗎

## AI

以下依您 4 個問題逐點回覆，並補上可直接落地的做法與資料來源。

---

## 1) PR 是什麼

**PR = Pull Request（拉取請求）**，是 GitHub 上最常見的協作與審查機制：  
當你在「分支」上完成一批程式修改後，透過 PR 把這些修改「提案」合併回主要分支（例如 main）。PR 會把「你改了什麼」用差異（diff）呈現出來，讓團隊在合併前進行討論、審查與確認。citeturn0search7turn0search3

在你目前情境（雛型 UI 快速迭代）中，PR 的實際價值是：  
- 把「每次 UI 調整」視為一個可回溯的變更包  
- 讓另一個「設計審查角色」有固定輸入（diff + 截圖/報告）可檢核  
- 後續若 style 走偏，可快速定位是哪一次變更造成

---

## 2) skill 是什麼？可以協助我做 AI Coding 嗎

你提到的「skill」，在 VS Code / Copilot 的語境中，多半指 **Agent Skills**：  
**Agent Skills 是一個資料夾集合**，裡面可以放「指令、腳本、資源與規範」，讓 Copilot 在需要時載入，用來執行特定任務（例如：UI 風格審查、前端重構規則、測試流程、提交訊息格式等）。citeturn0search0turn0search5

它能怎麼幫你做 AI Coding（含你要的「設計審查角色」）：
- **把“你要的 Notion-like / 內斂 B2B 風格”寫成規範檔**，讓 agent 每次改 UI 都依規範自我檢核  
- **把檢核清單（layout / typography / spacing / component states）固化**，避免模型每次審美標準漂移  
- **把常用命令與流程（跑 Playwright 截圖、產生 diff 報告）變成可重複任務**（後述第 4 點會接上 MCP / Browser automation）

補充：VS Code 的 Copilot Chat 也支援「工具（tools）」與「MCP tools」，讓 agent 不只是產 code，還能在你的允許下跑命令、抓資料、串工具。citeturn1search8turn1search12turn1search15

---

## 3) 雛型階段希望 coding 結果跟雛型一樣，怎麼做最穩

如果你的目標是「做出來必須長得跟雛型一致」，僅靠文字描述或僅靠 code review，風險仍高。建議你用 **“規範 + 視覺回歸”** 兩道保險：

### (A) 規範：把雛型變成“可檢核規格”
- 產出 `ui-style-guide.md`（字級階層、留白、圓角、陰影、表格密度、互動狀態）
- 產出 `ui-components-spec.md`（Button / Table / Card / Form 的 variants 與使用規則）
- 讓 agent 以此做 checklist 審查（這就是你想要的「設計審查角色」）

### (B) 視覺回歸：用 Playwright 讓「像不像」變成客觀結果
Playwright Test 提供 `expect(page).toHaveScreenshot()`：首次執行會建立 baseline，後續每次跑測試就會把目前畫面與 baseline 做視覺比對，並產出差異。citeturn1search1turn1search0turn1search3  
這非常適合你在雛型期把畫面“釘死”，確保後續 coding 不會越改越不像。

### (C) 元件化時的加乘：Storybook / Chromatic
若你有元件庫或準備做長期維護，Storybook 的 Visual tests 會把每個 story 的外觀快照與 baseline 比對；Chromatic 也提供 snapshots 與 UI Review/Tests 的整套流程。citeturn1search7turn1search17turn1search11turn1search14

---

## 4) LLM 可指揮瀏覽器點擊/輸入的工具，有幫助嗎

有幫助，但要用在「對的目的」，並理解其限制（安全性、可重現性、易受 UI 變動影響）。

### 4.1 什麼時候很有幫助
1) **把你“人工操作雛型驗收流程”轉成可重跑的腳本**（E2E 測試）  
2) **自動產生證據**：跑完流程後截圖、產報告，交給「Design Reviewer」判讀  
3) **加速迭代**：你改 UI → agent 跑流程 → 立即知道哪裡跑版/破壞一致性

### 4.2 你提到的「可被 LLM 指揮的瀏覽器」通常有兩類
- **Playwright + MCP**：例如 Microsoft 的 Playwright MCP server，讓 LLM 透過結構化方式與網頁互動（而非純截圖辨識）。citeturn0search6  
- **通用型 LLM browser agent 工具**：例如 browser-use 這類專案提供 LLM 驅動的瀏覽器自動化框架/流程。citeturn0search14turn0search10  

此外，VS Code 端也能透過 MCP servers 把這些能力納入 Copilot Chat 的工具鏈，形成「coding → 跑測試/跑瀏覽器 → 回報結果」的閉環。citeturn1search12turn1search15turn1search8

### 4.3 重要限制（建議你一開始就採用的管控方式）
- **可靠性**：UI 小改動（class、位置、文案）就可能讓點擊失敗；因此要配合穩定 selector（data-testid）與元件規範。  
- **安全性**：LLM 能操作瀏覽器等同能觸發具風險的動作；務必在本機/測試環境、最小權限、需要你 approve 的模式下使用。citeturn1search8turn1search12  
- **目的要清楚**：它最強在「自動化驗證與產生證據」，不建議把它當成唯一的 UI 審美判定來源；審美判定仍應回到「規範 + baseline」。

---

# 建議你下一步的最小可行落地（MVP）
1) **先做“設計審查角色”**：建立 `ui-style-guide.md` + 一份 Design checklist，放進 Agent Skills（或放 repo 規範檔供 Copilot 讀）。citeturn0search0  
2) **把 1–2 個關鍵頁面釘住**：加 Playwright `toHaveScreenshot()` 做 baseline/diff。citeturn1search1turn1search0  
3) **再考慮 LLM 控制瀏覽器**：當你想把「人工驗收路徑」自動化、並產出可審查證據時，再接 MCP / Playwright MCP。citeturn0search6turn1search12turn1search15  

如果你願意，我可以直接給你一份「可貼進 repo」的文件骨架（ui-style-guide + checklist + 建議的 Playwright 視覺回歸測試檔案結構），你只需要把你喜歡的雛型風格關鍵字（例如 Notion、Linear、Stripe 風）告訴我，我就能把規範條款寫到可用於審查的粒度。

